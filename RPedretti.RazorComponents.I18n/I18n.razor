@typeparam TKey
@using Models

@(Translations.TryGetValue(Translation.Key, out var translation) ? translation : Translation.Text)

@code
{
    [CascadingParameter] public ITranslations<TKey> Translations { get; set; }

    [Parameter] public ITranslation<TKey> Translation { get; set; }
}